svc "nginx" {
  state => "running",
  startup => "enabled",
} 

file "/etc/nginx/nginx.conf" {
  content => "# this is my nginx config",
  Depend => Pkg["nginx"]
  Notify => Svc["nginx"],
}

pkg "nginx" {
  state => "installed",
}
